package Exams.HashCollection;


public class AdvancedPractices {
    /**
     * Class name: AdvancedPractices
     *
     * Implement a method:
     *      boolean checkIfEqual(String str1, String str2)
     *
     * Description.md:
     *      Check whether the two given strings contain the same
     *      number of occurrences for every letter (a–z, case-insensitive).
     *
     *      Ignore non-letter characters.
     *
     * Examples:
     *   input:
     *      str1 = "Apple"
     *      str2 = "pPlea"
     *   output:
     *      true    // both contain: a=1, p=2, l=1, e=1
     *
     *   input:
     *      str1 = "hello"
     *      str2 = "heLLo!"
     *   output:
     *      true
     *
     *   input:
     *      str1 = "abc"
     *      str2 = "abcc"
     *   output:
     *      false
     *
     * Requirements:
     *   - Use a HashMap or an int[26] array to count letter frequencies.
     *   - Comparison should be case-insensitive.
     *   - Return true only if both strings have identical counts for all letters.
     *
     * @param str1 first input string
     * @param str2 second input string
     * @return true if both strings contain the same number of each letter
     */
    public boolean checkIfEqualIgnoreCase(String str1, String str2) {

        return false;
    }

    public boolean checkIfEqualNotIgnoreCase(String str1, String str2) {

        return false;
    }
}
