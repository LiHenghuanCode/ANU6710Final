package Exams.IO.IOPractice1;

import java.io.*;
import java.util.*;

/**
 * Reads a text file containing integers (one or more per line, separated by spaces),
 * and returns a list containing all even numbers found in the file, sorted in ascending order.
 *
 * Example file content:
 *   5 2 8 1 4
 *   3 6 10
 *
 * Expected result:
 *   [2, 4, 6, 8, 10]
 *
 * Requirements:
 *   - Use the provided I/O template.
 *   - Each line in the file may contain multiple integers separated by spaces.
 *   - Ignore invalid entries (non-numeric tokens).
 *   - Return an empty list if the file is empty or contains no even numbers.
 *
 * Use this template for file reading:
 * check the review then know how to read;
 *
 * @param file the input file to read integers from
 * @return a list of even integers sorted in ascending order
 */
public class ReadEvenNumbers {

    public static List<Integer> ReadEvenNumbersToFile(File file) {
        // TODO: Implement here
        return null;
    }
}
