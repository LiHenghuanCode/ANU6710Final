package Exams.IO.IOPractice1;

import java.io.*;
import java.util.*;

/**
 * Reads a list of integers and writes all even numbers
 * into a text file in ascending order.
 *
 * Each number should be written on a new line.
 *
 * Example:
 *   Input list: [5, 2, 8, 1, 4]
 *   Output file content:
 *       2
 *       4
 *       8
 *
 * Use the given I/O template:
 * try (var writer = new BufferedWriter(new FileWriter(file))) {
 *     ...
 * } catch (Exception e) {
 *     throw new RuntimeException(e);
 * }
 *
 * @param numbers the list of integers
 * @param file the file to write the even numbers into
 */
public class WriteEvenNumbers {

    public static void writeEvenNumbersToFile(List<Integer> numbers, File file) {
            // TODO: Implement here

    }
}

